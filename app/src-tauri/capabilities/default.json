{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default permissions for the main window (core APIs incl. events for dj-menu).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
